{
  "name": "sense-typing-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "font-decision-engine"
  ],
  "scripts": {
    "build": "npm install && npm run build -w font-decision-engine && npm run pages:build -w font-decision-engine"
  },
  "engines": {
    "node": ">=18"
  }
}
