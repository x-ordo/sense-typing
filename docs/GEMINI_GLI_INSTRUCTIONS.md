# 🔐 Gemini GLI 지시서 (Font Decision Engine Project)

## 0. 역할 정의 (가장 중요)

> 당신(Gemini GLI)은 **개발 보조자이자 실행 에이전트**입니다.
> **기획 변경, 구조 변경, 태그 의미 변경 권한은 없습니다.**
> 판단 기준(Taxonomy)은 이미 확정되었습니다.
> 당신의 임무는 **정확한 구현과 반복 작업 자동화**입니다.

---

## 1️⃣ 프로젝트 개요 전달 (Context Injection)

### Project Name

**Font Decision Engine**

### One-liner

> Fonts are free. **Decisions are premium.**

### Core Asset

* 감성/목적/리스크 기반 **Tag Taxonomy**
* 폰트 자체는 외부 링크만 제공
* **유료 대상은 태그와 조합 결과**

---

## 2️⃣ 기술 스택 (변경 금지)

* Next.js 14 (App Router)
* TypeScript
* Tailwind CSS
* Supabase (Postgres + Auth)
* Vercel Deploy
* 결제는 Phase 2 (지금 구현 ❌)

---

## 3️⃣ 보안 규칙 (절대 위반 금지)

### ❌ GitHub에 올리면 안 되는 것

* `assets/secret_taxonomy.xlsx`
* 전체 태그 점수 데이터
* seed.sql (폰트-태그 매핑 포함 시)

### ⭕ 허용

* 스키마
* 샘플 1~2개 폰트만 포함된 taxonomy_sample.md

---

## 4️⃣ DB 스키마 (이미 확정 – 그대로 사용)

```sql
create table fonts (
  id bigint generated by default as identity primary key,
  name text not null,
  developer text,
  license_summary jsonb,
  preview_url text,
  download_link text,
  created_at timestamp with time zone default timezone('utc', now())
);

create table tags (
  id bigint generated by default as identity primary key,
  name text not null,
  category text not null,
  intent text,
  is_premium boolean default false
);

create table font_tags (
  font_id bigint references fonts(id),
  tag_id bigint references tags(id),
  score int default 3,
  primary key (font_id, tag_id)
);
```

---

## 5️⃣ Gemini에게 맡길 작업 목록 (중요도 순)

---

### 🔥 TASK 1. Supabase Seed Script 생성 (Partial)

**목표**

* 전체 10개 폰트 ❌
* **샘플 2개 폰트만** seed.sql 생성

**조건**

* Pretendard
* Gmarket Sans
* 태그는 5~7개만
* 점수 포함
* 나머지는 주석 처리

👉 목적:
**구조 검증용**, 데이터 유출 방지

---

### 🔥 TASK 2. 태그 검색 API 구현

**파일**

```
src/app/api/fonts/route.ts
```

**요구사항**

* 입력: tag ids 배열
* 출력: 폰트 리스트
* 정렬: font_tags.score DESC
* premium tag 포함 시 flag 반환

```ts
{
  fonts: [],
  hasPremiumTag: true | false
}
```

---

### 🔥 TASK 3. Premium Lock 컴포넌트 구현

**컴포넌트명**

```
<PremiumLock />
```

**조건**

* is_premium 태그 + score ≥ 4 → 노출
* 폰트명 대신:

  * “검증된 선택지 잠금”
  * CTA 버튼: “판단 기준 잠금 해제”

---

### 🔥 TASK 4. TagChip UI 자동 생성

**조건**

* category별 색상 분리

  * Conversion: red
  * Risk/Compliance: purple
  * Vibe: blue
  * Format: gray
* premium tag → 🔒 아이콘 표시

---

### 🔥 TASK 5. README.md 작성 (Public Version)

**포함**

* What is this?
* What problem it solves
* Why not Noonnu
* What is NOT included

**금지**

* Taxonomy 상세
* 태그 수
* 점수 기준

---

## 6️⃣ UX 관련 명확한 규칙

### 무료 유저

* 태그 선택 가능
* 검색 결과 제한
* Premium tag 선택 시:

  * blur
  * 이름 비공개

### 유료 유저 (Phase 2)

* 태그 조합 추천
* 결과 수 제한 해제
* Risk 태그 활성

---

## 7️⃣ 절대 하지 말 것 (중요)

* ❌ “AI 추천이 더 좋을 것 같다”
* ❌ “태그를 더 추가하자”
* ❌ “유저가 직접 태그 달게 하자”
* ❌ “커뮤니티 만들자”

👉 이 프로젝트는 **집단 지성 ❌ / 중앙 판단 ⭕**

---

## 8️⃣ 작업 우선순위 (이 순서 그대로)

1. DB 연결 확인
2. 태그 검색 API
3. Premium Lock
4. TagChip
5. README

---

## 9️⃣ 커뮤니케이션 규칙

* 질문은 **Yes / No** 혹은 **A/B 선택형**으로만
* 판단 요청 ❌
* 구현 여부만 보고

---

## 10️⃣ 마지막 명령문 (Gemini에게 그대로 던져라)

> 이 프로젝트는 폰트를 파는 서비스가 아니다.
> **판단 기준을 파는 엔진**이다.
> 기획 변경 없이, 위 지시를 정확히 구현하라.
